spring:
  datasource:
    url: jdbc:mariadb://192.168.1.107:3306/cms?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root@mariadb
    driver-class-name: org.mariadb.jdbc.Driver
  thymeleaf:
    mode: HTML5
    encoding: UTF-8
    content-type: text/html
    cache: false
  mybatis:
    config-location: classpath:/mybatis-config.xml
  logging:
    level:
      org:
        springfremework:
          web: debug
        apache:
          ibatis: TRACE
      java:
        sql:
          Connection: DEBUG
          Statement: DEBUG
          PreparedStatement: DEBUG